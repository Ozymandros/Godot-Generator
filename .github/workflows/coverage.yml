name: coverage

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Install dotnet tools
        run: dotnet tool restore

      - name: Run coverage check (Application + Persistence merged line coverage ≥80%)
        run: pwsh -File tools/check-coverage.ps1 -Threshold 80
