@page "/settings/models"
@inject NavigationManager Nav
@rendermode InteractiveWebAssembly

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/settings?tab=2", replace: true);
    }
}
