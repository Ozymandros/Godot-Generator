# Blazor WebAssembly client (deprecated)

The primary UI for this solution is the **Avalonia** desktop app (`Godot-Generator`). It uses real file-backed persistence and the full AI + Godot MCP stack.

This Blazor WASM project remains as a reference implementation for in-browser stubs (`BrowserAiOrchestrationService`, `BrowserPreferenceRepository`) and is not the recommended production shell.

Use **Godot-Generator** for new work.
